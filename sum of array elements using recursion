#include<iostream>
using namespace std;
int sum(int n)
{
    int a[n],count=0,i;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
        count=count+a[i];
    }cout<<"Sum of an array is "<<count;
}
int main()
{
    int n;
    cin>>n;
    sum(n);
}
